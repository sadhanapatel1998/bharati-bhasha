export default function AdminSectionPage() {
  // The admin dashboard shell (with its own internal section/tab
  // navigation) is rendered by the parent admin layout for every
  // /admin/* route, so this page intentionally renders nothing.
  return null;
}
